# Copy to .env and adjust. If MONGO_URI is left blank, the server starts an
# embedded MongoDB (mongodb-memory-server) persisted to ./.mongo-data — zero setup.
MONGO_URI=
PORT=4000
JWT_SECRET=change-me-to-a-long-random-string
CLIENT_ORIGIN=http://localhost:5173
